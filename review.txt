

### Urgent Engineering Directive: Fixing Figure Metrics Discrepancies

**To:** Hermes Agent

**From:** Principal AI Architect

**Subject:** Critical Bug Fixes in Visualization Pipeline (`ela/viz.py`) Prior to Commit

Hermes,

We have detected two critical anomalies in the generated figures (`fig1_primary_bar.png` and `fig13_untrained_vs_pretrained.png`) during a visual sanity check. These discrepancies introduce logic violations that contradict our text and expose the paper to immediate rejection. You must patch the plotting logic in `ela/viz.py` and re-run the pipeline before committing.

Apply the following fixes immediately:

#### 1. Fix Label Alignment & Overlap in `fig1_primary_bar.png`

* **The Bug:** The labels on the X-axis are shifting. The bars are plotted sequentially rather than grouped symmetrically under each model tick. This makes it look like certain models exceed the 8-layer maximum protocol count.
* **The Fix:** Refactor the bar-plotting logic in `ela/viz.py` to use an explicit **Grouped Bar** implementation with symmetrical width offsets. Ensure that each model has exactly one tick on the X-axis, and the label is perfectly centered under its corresponding pair of Laplace and Gaussian bars.
```python
# Ensure this layout structure in your matplotlib call:
x = np.arange(len(models))
width = 0.35
ax.bar(x - width/2, laplace_wins, width, label='Laplace', color='#de425b')
ax.bar(x + width/2, gaussian_wins, width, label='Gaussian', color='#488f71')
ax.set_xticks(x)
ax.set_xticklabels(models, rotation=45, ha='right')

```



#### 2. Fix the False Positive 100% Random-Init Laplace Prevalence in `fig13_untrained_vs_pretrained.png`

* **The Bug:** For the encoder-decoder models (`t5-small`, `t5-base`, `google/mt5-small`), the gray bar (`Random init`) is erroneously showing at **100%**. This completely violates Section 4.4 of our manuscript, which accurately states that *all* models exhibit 0% Laplace prevalence at random initialization.


* **The Fix:** Trace the data dictionary/dataframe feeding `fig13_untrained_vs_pretrained.png`. Isolate why the random-init values for T5/mT5 are inheriting or duplicating the pretrained 100% metrics. Force an explicit validation check in the script: if a model is evaluated under `Random init`, its Laplace prevalence *must* correctly register its empirical 0% baseline value. The gray bars for those three models must be flat at zero.



---

### Execution Protocol

1. Modify `ela/viz.py` to resolve these plotting bugs.
2. Run `make clean-results` to purge the corrupted assets.
3. Execute `make pipeline` (or the specific script targets) to regenerate the correct, pristine versions of `fig1_primary_bar.png` and `fig13_untrained_vs_pretrained.png`.
4. Verify that all 47 tests still pass cleanly.
5. Once verified, stage all changes (including the refactored `README.md` from the previous step), commit, and push to `main`.

Deliver a final confirmation once the corrected plots are compiled and pushed!